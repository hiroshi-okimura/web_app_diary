<aside class="h-screen bg-gray-800 text-gray-100 w-64 space-y-6 px-2 py-7">
  <!-- User Info -->
  <div class="flex items-center space-x-3">
    <%= image_tag 'sample.jpg', class: "h-8 w-8 rounded-full" %>
    <div>
      <h2 class="text-lg font-semibold"><%= current_user.name %></h2>
    </div>
  </div>

  <!-- Navigation Menu -->
  <nav>
    <h2 class="text-xs font-semibold text-gray-400 tracking-wide uppercase">Main Navigation</h2>
    <ul class="mt-3 space-y-2">
      <li>
        <%= link_to "WebApp", admin_web_apps_path, class: "flex items-center space-x-3 text-gray-200 hover:text-gray-100" %>
        <%= link_to "User", admin_users_path, class: "flex items-center space-x-3 text-gray-200 hover:text-gray-100" %>
      </li>
      <!-- Other menu items here -->
    </ul>
  </nav>
</aside>